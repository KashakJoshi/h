#include <stdio.h>

int main() {
    
    int x[8], i, t;
    
    printf("Enter your numbers");
    
    for(i=0; i<8; i++)
    scanf("%d", &x[i]);
    
    t=x[0];
    for(i=1; i<8; i++)
    {
    if(x[i]>t)
    t=x[i];
    }
    printf("%d", t);
    return 0;
}
